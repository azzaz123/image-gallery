name: gallery_saver
description: Saves images and videos to the gallery.
version: 2.3.3
homepage: https://github.com/CarnegieTechnologies/gallery_saver

environment:
  sdk: '>=2.17.0 <4.0.0'
  flutter: '>=2.10.0'

dependencies:
  flutter:
    sdk: flutter
  path_provider: ^2.1.2
  http: ^0.13.6
  path: ^1.8.3

dev_dependencies:
  flutter_test:
    sdk: flutter
